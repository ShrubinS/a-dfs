Available operations:
    r       : read file. If file does not exist, throw error
    w       : overwrite file if exists, or create new file
    a       : modify existing file, file must already exist
    ls      : show files currently in directory
    cd      : move into directory, must provide absolute path
    help    : show list of available operations